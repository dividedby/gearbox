# Python bytecode (selfchecks, hook & bench scripts)
__pycache__/
*.pyc

# Gearbox per-project telemetry — the plugin writes this to cwd/.claude/
.claude/gearbox-log.jsonl
.claude/gearbox-baseline.txt

# Labeled training data produced by bench/label.py / bench/run-live.py — may
# contain prompt_head text from delegation prompts; kept out of version control
# for privacy.
bench/training-data.jsonl

# Per-run R3 cost/turns summary emitted by bench/run-live.py (transient output;
# copy its line into the agent-research cost ledger manually).
bench/last-run-summary.txt

# OS cruft
.DS_Store
